# --- App ---
spring.application.name=quesosbartolome
spring.mvc.pathmatch.matching-strategy=ant_path_matcher 

 
# --- DB ---
# spring.datasource.url=jdbc:mysql://localhost/quesosbartolome
# spring.datasource.username=root
# spring.datasource.password=Password0]
# spring.jpa.hibernate.ddl-auto=create-drop

spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:quesosbartolome}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:Password0]}
spring.jpa.hibernate.ddl-auto=create-drop



# HTTPS configurado
server.port=443
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=password
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=tomcat
server.ssl.key-password=password
